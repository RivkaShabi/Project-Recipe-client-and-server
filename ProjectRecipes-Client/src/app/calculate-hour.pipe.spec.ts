import { CalculateHourPipe } from './calculate-hour.pipe';

describe('CalculateHourPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateHourPipe();
    expect(pipe).toBeTruthy();
  });
});
